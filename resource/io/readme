BIO(同步阻塞) 缺点：在内核执行读操作和其他工作时，应用程序的确会被阻塞。 效率很低。
spinIO（同步非阻塞） 缺点：应用程序必须要进行忙碌等待，不断spin，查看内核是否响应。
        方法可以引入 I/O 操作的延时，因为数据在内核中变为可用到用户调用 read 返回数据之间存在一定的间隔，这会导致整体数据吞吐量的降低。

NIO（异步阻塞）缺点：select 调用的主要问题是它的效率不是非常高。尽管这是异步通知使用的一种方便模型，
但是对于高性能的 I/O 操作来说不建议使用。


AIO （异步非阻塞） 通过 标志 进行回调